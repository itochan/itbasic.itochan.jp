defaults: &defaults

development:
  <<: *defaults
  twitter:
    consumer_key: <%= ENV['TWITTER_CONSUMER_KEY'] %>
    consumer_secret: <%= ENV['TWITTER_CONSUMER_SECRET'] %>

test:
  <<: *defaults

production:
  <<: *defaults
